import React from 'react';
import ReactDOM from 'react-dom/client';
import BookstoreApp from './bookstore-app.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BookstoreApp />
  </React.StrictMode>,
);
